const styles = theme =>({
    root: {
        margin: theme.spacing(1),
        padding: theme.spacing(1),
        height: 'calc(100% - 2rem)'
    },
    icon: {
        minWidth: 0
    }
});

export default styles; 